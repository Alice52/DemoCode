package cn.edu.ntu.javase.enumeration.evolution.singleton;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/**
 * 1. Enumeration values are singleton, and each value in the enumeration is a singleton object,
 * <br>
 * and you can directly use == to determine the equivalent value. <br>
 * 2. Enumeration is the easiest way to define singleton objects. <br>
 *
 * @author zack <br>
 * @create 2020-01-31 16:12 <br>
 */
public class EnumSingle {
  private static final Logger LOG = LoggerFactory.getLogger(EnumSingle.class);
}
